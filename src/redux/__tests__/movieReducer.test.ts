import moviesReducer, { moviesState } from '../reducers/moviesReducer';

describe('Movies reducer', () => {
  test('Should return new state if action is passed', () => {
    expect(true).toBeTruthy();
  });
});
